{
  "name": "tradeal",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev:backend --prefix frontend",
    "dev:all": "npm run dev:all --prefix frontend",
    "dev:marketing": "npm run dev:marketing --prefix frontend",
    "verify:local": "npm run verify:local --prefix frontend",
    "build": "npm run build --prefix frontend",
    "build:marketing": "npm run build:marketing --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "preview": "npm run preview --prefix frontend",
    "preview:marketing": "npm run preview:marketing --prefix frontend"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
