# Local: leave VITE_API_URL unset so Vite proxies /api → http://127.0.0.1:8000
# Production (Vercel env, Production + Preview): Railway public URL.
# Either form works: origin only, or origin + /api/v1
# VITE_API_URL=https://your-service.up.railway.app/api/v1
